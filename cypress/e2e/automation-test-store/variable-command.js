/// <reference types="Cypress"/>

describe('Verifying variable, cypress commands and jquery command', () => {
    beforeEach('visit the web', () => {
        cy.visit('/')
    })

    it('Navigating to specific product pages', () => {})
})
